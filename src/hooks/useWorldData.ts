"use client";

/**
 * Phase 3: will load Convex queries and assemble `WorldData`.
 * Placeholder so routes and future components can import a stable hook name.
 */
export function useWorldData(): null {
  return null;
}
