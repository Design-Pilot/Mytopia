## Phase 7 Plan

- Add a simple shadow layer that draws soft ground ellipses under world entities while keeping the current layering and click behavior stable.
- Add one ambient motion system that uses the decoration renderer rather than introducing a separate animation stack.
- Make the demo world show the new motion clearly by updating or adding a small set of decorative sample entities.
- Verify the result with lint, a production build, and a browser sanity pass.
- Finish with a fresh handoff note that explains what is done, what is still open, and the most sensible next step.
